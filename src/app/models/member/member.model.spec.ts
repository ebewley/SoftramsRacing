import { IMember } from './member.model';

describe('Member.Model', () => {
  it('should create an instance', () => {
    expect(<IMember>{}).toBeTruthy();
  });

  // TODO: Don't forget to test that all properties can be set and read from a Member object.
  // TODO: Don't forget to test that a JSON object can be cast as a Member object.
});